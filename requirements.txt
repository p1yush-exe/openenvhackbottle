 Validation passed in your medagentgym Conda env: pip install -e . worked, import medagent_env worked, and from server.app import app returned a FastAPI app.
  Docker build could not be completed because the local Docker daemon is not running on this machine.

  Use:

  conda activate medagentgym
  cd "C:\Users\LENOVO\OneDrive\Desktop\build a GF\PROJECTS\openenv\medagent_env"
  pip install -e .
  uvicorn server.app:app --reload --port 8000

  For Docker, once Docker Desktop/daemon is running:

  docker build -t medagent_env:latest .
  docker run -p 8000:8000 medagent_env:latest
